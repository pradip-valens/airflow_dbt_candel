name: "vibe_ops_fabric_wh_pr"

config-version: 2
version: "0.1"

profile: "vibe_ops_fabric_wh_pr"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"
  - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

models:
  vibe_ops_fabric_wh_pr:
    brz:
      schema: brz_jira_sm
      materialized: table
    slv:
      schema: slv_jira_sm
      materialized: table
    gold:
      schema: gold_jira_sm
      materialized: table
